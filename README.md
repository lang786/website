# 360 eLearning Website

Modern Next.js website for 360 eLearning.

## Run Locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build

```bash
npm run build
```

The static site will be generated in the `out` folder. Upload the contents of `out` to your webserver test folder.
